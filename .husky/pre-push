#!/usr/bin/env bash
set -euo pipefail

if [[ "${HUSKY:-}" == "0" || "${SKIP_PREPUSH:-}" == "1" ]]; then
  echo "[husky] pre-push skipped"
  exit 0
fi

npm run lint --if-present
npm run typecheck --if-present